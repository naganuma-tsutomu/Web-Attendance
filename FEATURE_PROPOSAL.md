# Web-Attendance 追加機能提案書

作成日: 2026-05-12  
対象: Web-Attendance v2.5.0

## 1. 背景

Web-Attendance は、スタッフの希望休収集、シフト自動生成、日/週/月表示での調整、Excel 出力までを一通りカバーしている。既存機能は実運用に近い状態まで整っており、今後の改善は「生成結果を安全に扱う」「外部データを取り込む」「生成結果の理由を説明する」方向が効果的と考えられる。

本提案では、特に運用事故を減らし、管理者の作業負荷を下げる追加機能を優先して整理する。

## 2. 提案サマリー

| 優先度 | 機能 | 目的 | 推定規模 |
|---|---|---|---|
| 高 | シフト情報のバックアップ/復元 | 自動生成・消去・手修正前の状態に戻せるようにする | 中 |
| 高 | 外部ファイルからのシフト取り込み | 既存 Excel/CSV 運用から移行しやすくする | 中 |
| 中 | 生成前チェック/生成後レポート | 未割当や偏りの理由を管理者が把握しやすくする | 中 |
| 中 | 希望休の提出期限設定 | スタッフ側の入力締切をシステムで制御する | 小〜中 |
| 中 | 変更履歴/監査ログ | 誰がいつ何を変更したか追跡できるようにする | 中 |
| 低 | PDF 出力の再開 | 現在準備中の機能を完成させる | 中 |

## 3. 優先提案

### 3.1 シフト情報のバックアップ/復元

#### 目的

自動生成や月シフト消去、手動調整の前後で状態を保存し、問題が起きた場合にすぐ戻せるようにする。

現状、自動生成では月の既存シフトを置換するため、生成結果が意図しない場合の戻し操作が弱い。バックアップ機能を追加することで、管理者が安心して生成や調整を実行できる。

#### 想定仕様

- 月単位でシフトスナップショットを保存する。
- 自動生成前、月シフト消去前に自動バックアップを作成する。
- 管理者が任意のタイミングで「バックアップ作成」できる。
- バックアップ一覧から対象月へ復元できる。
- 復元前にも現在状態を自動バックアップする。

#### DB 案

```sql
CREATE TABLE IF NOT EXISTS shift_snapshots (
    id TEXT PRIMARY KEY,
    yearMonth TEXT NOT NULL,
    label TEXT,
    reason TEXT NOT NULL,
    shifts_json TEXT NOT NULL,
    created_at TEXT DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_shift_snapshots_ym_created
ON shift_snapshots(yearMonth, created_at);
```

#### UI 案

- シフト画面ヘッダーに「バックアップ」ボタンを追加。
- モーダルで以下を表示する。
  - バックアップ作成
  - バックアップ一覧
  - 作成日時
  - 理由
  - 件数
  - 復元ボタン

#### 実装順

1. `shift_snapshots` テーブルとマイグレーションを追加。
2. `GET /api/shifts/snapshots?yearMonth=YYYY-MM` を追加。
3. `POST /api/shifts/snapshots` を追加。
4. `POST /api/shifts/snapshots/:id/restore` を追加。
5. 自動生成前と消去前に自動バックアップを作成。
6. 管理画面にバックアップ UI を追加。

### 3.2 外部ファイルからのシフト取り込み

#### 目的

既存の Excel/CSV 運用で作成したシフト情報を Web-Attendance に取り込めるようにする。導入初期や他システムとの併用時に特に有効。

#### 推奨する初期仕様

まずは CSV 取り込みから開始する。Excel 取り込みは、CSV 版の仕様が固まった後に追加する。

#### CSV フォーマット案

| 列名 | 必須 | 例 |
|---|---|---|
| date | 必須 | 2026-06-01 |
| staffName | 必須 | 山田太郎 |
| className | 必須 | 受付 |
| startTime | 必須 | 09:00 |
| endTime | 必須 | 18:00 |
| dutyNumber | 任意 | 1 |

日本語列名にも対応する場合:

| 日本語列名 | 対応フィールド |
|---|---|
| 日付 | date |
| スタッフ名 | staffName |
| クラス | className |
| 開始 | startTime |
| 終了 | endTime |
| 当番番号 | dutyNumber |

#### 取り込みフロー

1. ファイルを選択。
2. クライアント側で CSV を解析。
3. スタッフ名、クラス名、日付、時刻を検証。
4. プレビュー画面で取り込み結果を表示。
5. エラー行があれば保存不可にする。
6. 取り込み方式を選択する。
   - 既存シフトに追加
   - 対象月を置換
   - 固定日は保持して置換
7. 保存前にバックアップを作成する。

#### 検証項目

- 日付が `YYYY-MM-DD` 形式であること。
- 対象月の範囲内であること。
- スタッフ名が登録済みであること。
- クラス名が登録済みであること。
- 開始/終了時刻が `HH:MM` 形式であること。
- 同一スタッフの同日同時間帯に重複がないこと。
- 当番番号が同じ日付で重複しないこと。

### 3.3 生成前チェック/生成後レポート

#### 目的

自動生成の結果に未割当が出たとき、管理者が原因を理解しやすくする。現在は未割当件数は分かるが、理由の分類が弱いため、修正すべき設定や希望休を見つけにくい。

#### 生成前チェック案

- 必要人数に対して候補者が少ない日を表示。
- 希望休が集中している日を表示。
- クラス所属スタッフが不足している日を表示。
- 対象スタッフ区分の勤務パターンが不足している日を表示。

#### 生成後レポート案

- 未割当一覧。
- 未割当理由の分類。
- スタッフ別の月間勤務時間。
- スタッフ別の週間勤務時間。
- 目標時間との差分。
- クラス別の充足率。

#### 未割当理由の分類例

- 希望休または部分休と重なった。
- 固定休または休館日だった。
- 勤務パターンが時間帯に対応していない。
- 月間目標時間を超過する。
- 週間目標時間を超過する。
- 対象クラスに所属していない。
- すでに同時間帯に別シフトがある。

## 4. 追加提案

### 4.1 希望休の提出期限設定

#### 目的

月ごとに希望休の提出期限を設定し、締切後はスタッフ側で編集できないようにする。

#### 仕様案

- 管理者が年月ごとに提出期限を設定。
- スタッフ画面では締切日時を表示。
- 締切後はスタッフ側の保存ボタンを無効化。
- 管理者は締切後も編集可能。
- 締切未提出スタッフを一覧表示。

#### DB 案

```sql
CREATE TABLE IF NOT EXISTS preference_deadlines (
    yearMonth TEXT PRIMARY KEY,
    deadline_at TEXT NOT NULL,
    updated_at TEXT DEFAULT (datetime('now'))
);
```

### 4.2 変更履歴/監査ログ

#### 目的

シフトや設定の変更履歴を残し、運用上の問い合わせや誤操作の原因確認に使えるようにする。

#### 記録対象

- シフト自動生成。
- シフトの手動追加、変更、削除。
- 月シフト消去。
- バックアップ作成、復元。
- 固定日の変更。
- スタッフ情報の変更。
- 必要人数設定の変更。

#### DB 案

```sql
CREATE TABLE IF NOT EXISTS audit_logs (
    id TEXT PRIMARY KEY,
    actor_type TEXT NOT NULL,
    actor_id TEXT,
    action TEXT NOT NULL,
    target_type TEXT NOT NULL,
    target_id TEXT,
    summary TEXT,
    payload_json TEXT,
    created_at TEXT DEFAULT (datetime('now'))
);
```

### 4.3 PDF 出力の再開

#### 目的

現在 UI 上で準備中となっている PDF 出力を完成させる。

#### 方針

Excel 出力がすでに充実しているため、PDF は「印刷・配布用の簡易版」と位置付けるのがよい。

- 月間一覧。
- スタッフ別一覧。
- 休みスタッフの表示は最低限。
- レイアウトは A4 横を基本にする。

## 5. 推奨ロードマップ

### Phase 1: 安全性の強化

1. シフトバックアップ/復元。
2. 自動生成前、消去前の自動バックアップ。
3. 復元 UI。

この段階で、生成や一括操作を安心して使える状態にする。

### Phase 2: 入力経路の拡張

1. CSV 取り込み。
2. 取り込みプレビュー。
3. エラー行表示。
4. 置換/追加/固定日保持の選択。

この段階で、外部ファイルからの移行や併用をしやすくする。

### Phase 3: 生成品質の可視化

1. 生成後レポート。
2. 未割当理由の分類。
3. 目標時間との差分表示。

この段階で、自動生成後の調整作業を短縮する。

### Phase 4: 運用管理の強化

1. 希望休提出期限。
2. 未提出スタッフ一覧。
3. 変更履歴/監査ログ。

この段階で、日常運用の管理性を高める。

## 6. 最初に着手すべき機能

最初に実装するなら、シフト情報のバックアップ/復元を推奨する。

理由:

- 自動生成、消去、外部取り込みのすべてに対する安全網になる。
- 既存の月単位シフト置換フローと相性がよい。
- 管理者の心理的負担をすぐに下げられる。
- 今後 CSV 取り込みを追加する際にも、取り込み前バックアップとして再利用できる。

次点で CSV 取り込みを追加すると、既存の Excel/CSV 運用からの移行がかなり楽になる。
