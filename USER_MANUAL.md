# Web-Attendance v2.4.0 ユーザーマニュアル

本マニュアルは、シフト管理システム「Web-Attendance」v2.3.2 の使い方を説明します。本システムは**スタッフ向け機能**と**管理者（シフト作成者）向け機能**の2つに分かれています。

---

## 目次

1. [システム概要](#1-システム概要)
2. [スタッフ向けの使い方](#2-スタッフ向けの使い方)
   - [ログイン](#21-スタッフ用ログイン)
   - [希望休の提出](#22-希望休の提出)
   - [シフトの確認](#23-シフトの確認)
   - [ログアウト](#24-ログアウト)
3. [管理者向けの使い方](#3-管理者向けの使い方)
   - [ログイン](#31-管理者用ログイン)
   - [初期設定](#32-初期設定)
   - [スタッフ管理](#33-スタッフ管理)
   - [希望休の管理](#34-希望休の管理管理者用)
   - [シフトの作成と調整](#35-シフトの作成と調整)
   - [シフトのエクスポート](#36-シフトのエクスポート)
   - [当番番号設定](#37-当番番号設定)
   - [Excel出力設定](#38-excel出力設定)
   - [ローテーション設定](#39-ローテーション設定)
   - [休憩設定](#310-休憩設定)
4. [トラブルシューティング](#4-トラブルシューティング)

---

## 1. システム概要

### 1.1 システムの構成

トップページ（`/`）にアクセスすると、2つの入口が表示されます。

| 入口 | 対象 | 説明 |
|------|------|------|
| **スタッフ用ポータル** | 一般スタッフ | 希望休の提出・シフトの確認 |
| **管理者用ポータル** | シフト管理者 | スタッフ管理・シフト作成・各種設定 |

### 1.2 v2.0 の主な新機能

- スタッフが自分でログインして希望休を申請できるポータルを追加
- 希望休で「終日休」と「時間帯指定（一部不可）」を選択可能に
- 管理画面から営業時間・休館日を設定可能に
- シフト確定（ロック）機能の追加
- シフトのタイムライン表示（バー表示）をスタッフページにも追加
- 役職名を「スタッフ区分」に統一

### 1.3 v2.1 の変更点

- **アクセスキーを6桁に変更**: スタッフログインに使用するアクセスキーが4桁から6桁になりました（既存のキーは管理者が再設定してください）
- **Excel出力の改善**: ファイル名が `シフト表_YYYY-MM.xlsx` 形式に統一され、先頭に年月のタイトル行が追加されました。また、休日スタッフの表示が希望休・固定休・研修で色分けされ、定休日のスタッフは除外されるようになりました
- **Excel出力設定の追加**: 管理画面に「Excel出力設定」メニューが追加されました。土曜日の休日スタッフ非表示や、スタッフ別のシフトハイライトルールを設定できます
- **PDF出力を一時停止**: PDF出力ボタンは現在無効化されています
- **モバイル対応の改善**: 月間カレンダーのスクロール対応、スマホでの希望休管理画面のスタッフ選択をドロップダウン形式に変更
- **スタッフ・時間パターンの初期値改善**: 新規作成時に営業日設定に基づいた曜日が自動選択されるようになりました

### 1.4 v2.2.1 の変更点

- **UIの全体的な改善**: ボタンやアイコンにマウスを乗せた際、確実にポインター（指マーク）に変わるよう改善されました
- **週ビューの表示改善**: 週間のシフト表で、事業所の定休日は表示から除外されるようになりました
- **日ビューのドラッグ操作修正**: 1日のタイムライン上でのシフトのドラッグ移動・時間変更がスムーズに行えるよう修正されました
- **アクセスキー再発行時の警告**: スタッフのアクセスキーを再発行する際、古いキーが使えなくなる旨の確認ダイアログが表示されるようになりました
- **設定画面のデザイン統一とモバイル対応**: 「クラス」「スタッフ区分」「時間パターン」「Excel出力設定」の各設定画面がカード型の統一されたデザインになり、スマートフォンなどの小さな画面でも見やすくレイアウトが調整されました
- **マニュアルのナビゲーション**: ユーザーマニュアルの目次項目をクリックすると、自動で該当セクションまでスクロールするようになりました

### 1.5 v2.3.0 の変更点

- **固定休日の表示改善**: スケジュールの非勤務セクションにおいて、固定休日（定休日）の表示スタイルとラベルが改善され、より見やすくなりました
- **カスタム日付ピッカー**: スケジュール画面の日付ナビゲーションが、カレンダーポップアップ付きの新しい日付ピッカーに変更されました。月の切り替え、今日・選択中の日付の強調表示、日本語ロケール対応を備えています
- **表示設定の手動保存**: カラーテーマと週の開始曜日の設定が、変更時の自動保存から「保存」ボタンによる手動保存に変更されました。保存時にはトースト通知で結果が表示されます
- **セッションのセキュリティ強化**: スタッフのログインセッションが、より安全な HttpOnly Cookie 方式に変更されました。利用方法に変更はありません
- **ローテーション設定の追加**: 早番・遅番のローテーションを自動化する設定が追加されました。対象スタッフ区分・早番/遅番パターン・人数・土曜日の配置ルールを設定でき、前日の遅番が翌日の早番に回るローテーションが自動生成されます
- **休憩時間の自動計算**: 労働基準法に基づく休憩時間の自動計算機能が追加されました。例外ルール（早朝シフト向け）の設定や、シフトモーダル・Excel出力での実労働時間表示にも対応しています

### 1.6 v2.3.1 の変更点

- **スタッフシフト確認画面の改善**: スマートフォンでも日付ナビゲーションのサイドバーが表示されるようになりました。青ハイライトが当日固定に戻り、日付をタップすると該当日までスムーズスクロールします
- **月ナビゲーターの固定表示**: スタッフのシフト確認画面で、月切り替えナビゲーターがスクロールしても常に上部に固定（すりガラス風デザイン）されるようになりました
- **希望休の提出状態管理の改善**: 管理者が希望休を保存しても自動的に「提出済み」にならないよう変更されました。管理者は「提出済みにする」ボタンで明示的に提出状態を切り替えられます。スタッフ側の保存は従来通り提出済みとしてマークされます
- **UIの細かな修正**: スマートフォンでのモーダル表示位置（画面中央に表示）、下タブの選択背景幅など各所のUIが改善されました
- **Excel出力の修正**: 土曜日に複数行ある場合、2行目以降にも青背景が正しく適用されるようになりました

### 1.7 v2.3.2 の変更点

- **営業時間の時刻設定が30分刻みに対応**: 初期設定の営業時間（開始・終了時刻）を30分単位で設定できるようになりました
- **土曜日ローテーションに専用パターンを設定可能**: ローテーション設定の土曜日出勤について、平日とは別の専用シフトパターン（早番・遅番）を指定できるようになりました
- **スタッフ用「設定」タブからダークモードを切り替え可能**: スタッフポータルの「設定」タブにダークモードの切り替えスイッチが追加されました
- **通知の表示位置を改善**: 希望休の保存成功・エラーなどの通知メッセージが、画面右上のトースト通知として表示されるようになりました（以前のインライン表示から変更）
- **希望休入力モーダルのUI改善（特にスマートフォン向け）**: 希望休の入力モーダルがよりコンパクトになり、iPhoneなど小さな画面でも操作しやすくなりました
- **スタッフ情報編集画面のUX改善**: スタッフ情報の更新モーダルで、入力操作がより使いやすくなり、固定休日のレイアウトも整理されました
- **iOSでの入力ズーム問題を修正**: iPhoneで時間や名前などを入力する際に、画面が意図せず拡大される問題が修正されました
- **スマートフォンでのUIレイアウト修正**: モバイルでの凡例崩れ、クラス名入力時の自動フォーカス、勤務パターン編集モーダルの時間入力欄のはみ出しなど、スマートフォン表示の各種崩れを修正しました
- **シフト表示の横スクロールバー問題を修正**: 閲覧専用（readOnly）モードで横スクロールバーが表示されてしまう問題を修正しました

### 1.8 v2.4.0 の変更点

- **当番番号（リーダー番号）機能の追加**: シフト表とExcel出力に「1, 2, 3 …」の当番番号列を表示できるようになりました（`設定 > 表示設定`）。番号は日付ベースのローテーションで自動計算され、日ビューで個別に上書き・スワップも可能です
- **1番ローテーション対象区分の指定**: 「1番」（リーダー番号）を回す対象スタッフ区分を、ドロップダウンから自由に選択できるようになりました（旧「正社員のみで回す」トグルから変更）。「制限なし」を選ぶと従来通り全スタッフでローテーションします
- **クラス間自動スワップによる1番補正**: シフト自動生成の最終ステップで、対象区分のスタッフがいないクラスがある場合に、別クラスから同時刻のスタッフを自動入替えして1番が必ず対象区分になるよう調整するようになりました（必要人数や所属クラス制約は維持されます）
- **クラス管理ページの統合リデザイン**: クラスの並び替え・編集UIが刷新され、操作しやすくなりました
- **ヘルプクラスの自動割り当て修正**: ヘルプクラスへ誤って自動割り当てされる不具合を修正し、UIに割り当て対象切替トグルを追加しました
- **PWA対応**: ホーム画面への追加・オフライン利用に対応しました。アップデートが利用可能な場合、モバイルヘッダーの更新ボタンから即座に反映できます
- **設定保存後のキャッシュ即時反映**: 必要人数・クラス・スタッフ区分の保存後、画面表示が即時に更新されるようになりました
- **シフトパターン選択UIの修正**: シフトパターンのセレクトボックスで、文字とドロップダウンの矢印が重なる問題を修正しました
- **時間帯設定の表示修正**: 時間帯設定の「〜」記号がラベルと重なる問題を修正しました

---

## 2. スタッフ向けの使い方

### 2.1 スタッフ用ログイン

1. ブラウザでシステムのURL（例: `https://[システムドメイン]/`）にアクセスします。
2. 「スタッフ用ポータル」ボタンをクリックします。
3. ログイン画面が表示されます。
   - **名前**: ドロップダウンから自分の名前を選択します。
   - **アクセスキー**: 管理者から案内された数字のキーを入力します。
4. 「ログイン」ボタンをクリックします。

> **ログインできない場合**: 名前の選択ミス、またはアクセスキーの入力ミスが考えられます。管理者に確認してください。

---

### 2.2 希望休の提出

ログイン後、「希望休」タブが表示されます。毎月の提出期限までに希望を入力してください。

#### カレンダーの見方

| 表示 | 意味 |
|------|------|
| 白背景 | 出勤可能（未入力） |
| 赤背景「休」 | 終日休み希望 |
| 赤背景「部分休」 | 一部時間の休み希望 |
| 灰色（操作不可） | 固定休日（毎週の休み）または研修日 |
| 紺色の点 | すでにシフトが割り当てられている日 |

#### 希望を入力する

1. カレンダー上の日付をクリックすると、選択メニューが表示されます。
2. 以下から選択します。

**終日お休み**
- その日を丸1日休みとして申請します。

**一部の時間だけ不可**
- 出勤できない時間帯（開始時刻〜終了時刻）を指定します。
- 例: 午前中は用事があるため「09:00〜12:00 は不可」と指定する。

**就業可能（クリア）**
- すでに入力した希望休を取り消します。

#### 保存する

- 入力が終わったら「希望を保存」ボタンをクリックします。
- 提出期限前であれば何度でも修正できます。
- 「キャンセル」をクリックすると、未保存の変更を破棄できます。

---

### 2.3 シフトの確認

「シフト確認」タブでは、確定済みのシフトをタイムライン形式で確認できます。

- **自分のシフト**: ハイライト表示されます。
- **他のスタッフのシフト**: 同じ日の全員のシフトが表示されます。
- 月の切り替えは画面上部の矢印ボタンで行います。

---

### 2.4 ログアウト

「設定」タブを開き、「ログアウト」ボタンをクリックします。

---

## 3. 管理者向けの使い方

### 3.1 管理者用ログイン

1. トップページで「管理者用ポータル」ボタンをクリックします。
2. メールアドレスとパスワードを入力してログインします。

ログイン後、左側のメニューから各機能にアクセスできます。

---

### 3.2 初期設定

シフト作成を始める前に、設定メニューから事業所のルールを登録してください。

#### 時間パターン設定（`設定 > 時間パターン`）

「早番」「遅番」などの勤務時間帯を登録します。シフト作成時やスタッフの希望入力時に選択肢として使用されます。

| 項目 | 説明 |
|------|------|
| パターン名 | 例: 「早番」「遅番」 |
| 開始・終了時刻 | 例: 09:00 〜 18:00 |
| 対応曜日 | 利用可能な曜日にチェック |
| 対象スタッフ区分 | このパターンを使用できるスタッフ区分を選択 |

並び順はドラッグ&ドロップで変更できます。

#### スタッフ区分設定（`設定 > スタッフ区分`）

「正社員」「パート」などのスタッフ区分を登録します。

| 項目 | 説明 |
|------|------|
| 区分名 | 例: 「正社員」「アルバイト」 |
| 月間目標時間 | この区分の月間の目標勤務時間 |
| 週間目標時間 | この区分の週間の目標勤務時間 |
| 利用可能パターン | 使用できる時間パターンにチェック |

#### クラス設定（`設定 > クラス`）

業務の種類や担当グループを登録します。シフト表の表示分類に使用されます。

| 項目 | 説明 |
|------|------|
| クラス名 | 例: 「受付」「講師」 |
| 表示色 | タイムラインバーの色 |
| 自動配置 | 自動生成時に対象とするか |

#### 必要人数設定（`設定 > 必要人数`）

曜日・時間帯ごとの最低配置人数を登録します。シフト自動生成の基準として使用されます。

| 項目 | 説明 |
|------|------|
| 曜日 | 毎日 / 平日 / 特定曜日から選択 |
| クラス | 対象のクラス |
| 開始・終了時刻 | 必要人数が適用される時間帯 |
| 最低人数 | この時間帯に必要なスタッフ数 |
| 優先度 | 高いほど自動生成時に優先される（1〜5） |

並び順はドラッグ&ドロップで変更できます。

---

### 3.3 スタッフ管理

左メニューの「スタッフ」から、スタッフ情報を管理します。

#### スタッフの登録・編集

「新規追加」ボタンまたは各スタッフ行の編集アイコンをクリックします。

| 項目 | 説明 |
|------|------|
| 名前 | スタッフの氏名 |
| スタッフ区分 | 正社員・パートなどの区分 |
| 月間目標時間 | 個別に設定する場合（未入力の場合は区分のデフォルト値） |
| 週間目標時間 | 個別に設定する場合 |
| 所属クラス | 担当する業務クラス（複数選択可） |
| アクセスキー | スタッフがログインに使用する6桁の数字キー |

#### スタッフの並び替え

スタッフ一覧の各行の左端にあるアイコン（⠿）をドラッグして並び替えます。シフト表の表示順に反映されます。

#### スタッフの削除

編集画面またはスタッフ行の削除アイコンから削除できます。確認ダイアログが表示されます。

---

### 3.4 希望休の管理（管理者用）

左メニューの「希望休」から、スタッフが提出した希望休を確認・修正できます。

1. 画面上部でスタッフを選択します。
2. 対象月を選択します（前月・翌月への切り替えボタン使用）。
3. カレンダーに選択したスタッフの希望休が表示されます。
4. 日付をクリックして、希望内容の確認・修正ができます。

---

### 3.5 シフトの作成と調整

左メニューの「スケジュール」からシフト管理を行います。

#### ビューの切り替え

画面上部のタブで表示形式を切り替えられます。

| ビュー | 説明 |
|--------|------|
| 月ビュー | カレンダー形式で1ヶ月のシフトを表示 |
| 週ビュー | 週単位のタイムラインで表示 |
| 日ビュー | 1日のタイムラインで詳細な編集が可能 |

#### シフトの自動生成

1. 画面右上の「生成」ボタンをクリックします。
2. 確認ダイアログが表示されます（既存のシフトは上書きされます）。
3. 「実行」をクリックすると、以下の条件を考慮してシフトが自動生成されます。
   - スタッフが提出した希望休
   - 各スタッフのスタッフ区分と利用可能な時間パターン
   - 必要人数設定のルール
   - ロック済みの日付（後述）のシフトは保持されます

#### シフトの手動編集（日ビュー）

日ビューでは以下の操作が可能です。

**シフトの追加**
- 「追加」ボタンをクリックし、スタッフを選択します。
- 日付・開始時刻・終了時刻を設定して保存します。

**シフトの入れ替え**
- 既存のシフトバーをクリックし、「入れ替え」を選択します。
- 他のスタッフを選択すると入れ替えが実行されます。

**シフト時間の調整**
- タイムライン上のバーをドラッグして開始・終了時刻を変更できます（15分単位）。
  - バーをドラッグ: 開始時刻の変更
  - Alt + ドラッグ: 終了時刻の変更

**シフトの削除**
- シフトバーをクリックし、削除アイコンをクリックします。確認後に削除されます。

#### シフトのロック（確定）

特定の日付のシフトを確定（ロック）すると、次回の自動生成時に上書きされなくなります。

- 日ビューの日付ヘッダーにあるロックアイコンをクリックしてロック/解除を切り替えます。
- ロック済みの日付はアイコンが変わり、自動生成から除外されます。
- ロック後も管理者による手動編集は可能です。

#### シフトの一括クリア

「クリア」ボタンをクリックすると、表示中の月のシフトを全削除できます（ロック済みの日付は除外されます）。

---

### 3.6 シフトのエクスポート

確定済みのシフトをファイルとして出力できます。

| 形式 | 説明 |
|------|------|
| **Excel** | タイムライン付きの詳細なシフト表（.xlsx 形式）。ファイル名は `シフト表_YYYY-MM.xlsx` 形式 |
| **PDF** | 現在は一時停止中（ボタン無効） |

画面右上の「Excel」ボタンからダウンロードできます。

Excel出力には以下の情報が含まれます。

- 先頭行に年月のタイトル
- 出勤スタッフのシフトバー（時間ごとの細線付き）
- 休日スタッフの情報（希望休・固定休・研修を色分けで表示）
- 定休日のスタッフは行から除外

> Excel出力のカスタマイズは「**設定 > Excel出力設定**」から行えます（次節参照）。

---

### 3.7 当番番号設定

`設定 > 表示設定` の「当番番号設定」セクションから、シフト表とExcel出力に表示する **当番番号（1, 2, 3 …）** を制御できます。

#### 番号カラムの表示

| 設定 | 説明 |
|------|------|
| **番号カラムを表示する** | ONにすると、日ビューの各クラスとExcel出力の出勤行の左側に当番番号列が表示されます |

番号は基準日（2024-01-01）からの経過日数とスタッフの並び順をもとに、クラスごとに独立してローテーション計算されます（毎日 1, 2, 3 …）。日ビューでは番号セルをクリックして手動で入れ替え（スワップ）も可能です。

#### 1番ローテーション対象区分

| 設定 | 説明 |
|------|------|
| **1番を特定区分のみで回す** | ドロップダウンで対象区分を選択。選択した区分のスタッフだけが「1番」を循環し、その他のスタッフは2番以降に並びます。「制限なし」では全員が均等にローテーションします |

> **クラス間スワップによる自動補正**: シフト自動生成の最終ステップで、対象区分のスタッフが0人になっているクラスがあると、別クラスから同時刻（開始・終了が完全一致）のスタッフを自動的に入替えて、各クラスに必ず対象区分のスタッフが含まれるよう調整します。所属クラス制約や必要人数は維持されるため、シフトの整合性は崩れません。

---

### 3.8 Excel出力設定

`設定 > Excel出力設定` から、Excel出力の見た目をカスタマイズできます。

#### 土曜日の表示制御

| 設定 | 説明 |
|------|------|
| **土曜日の休日スタッフを非表示にする** | ONにすると、土曜日の行には出勤スタッフのみが表示されます（休日のスタッフ行は省略） |

#### スタッフ別シフトハイライト

特定のスタッフが「通常の勤務時間」と異なるシフトに入った際、Excel上でその行を指定した色で強調表示するルールを設定できます。

**ルールの追加手順**

1. 「ルールを追加」ボタンをクリックします。
2. 対象スタッフ・通常の勤務時間（開始〜終了）・ハイライト色を設定します。
3. 「設定を保存」ボタンをクリックします。

> **ルールの動作**: 設定した「通常の勤務時間」と **一致しない** シフトが組まれた場合のみ、その行が指定色で塗りつぶされます。通常通りのシフトには色がつきません。

---

### 3.9 ローテーション設定

`設定 > ローテーション` から、早番・遅番の自動ローテーションを設定できます。

#### 基本設定

| 項目 | 説明 |
|------|------|
| **有効/無効トグル** | ローテーション機能全体のON/OFF |
| **対象スタッフ区分** | ローテーションを適用するスタッフ区分を選択 |
| **早番パターン** | 早番に使用する時間パターン（例: 06:00〜15:00） |
| **遅番パターン** | 遅番に使用する時間パターン（例: 14:00〜23:00） |

#### 平日体制

| 項目 | 説明 |
|------|------|
| **早番人数** | 平日（月〜金）の早番スタッフ数 |
| **遅番人数** | 平日（月〜金）の遅番スタッフ数 |

> **ローテーションの仕組み**: 前日の遅番スタッフが翌日の早番に、前日の早番スタッフが翌日の遅番にローテーションします。休みなどでローテーションが維持できない場合は、累計時間バランスで自動調整されます。

#### 土曜日設定

| 項目 | 説明 |
|------|------|
| **土曜日の出勤** | 土曜日のスケジューリングを有効にする |
| **土曜日の出勤人数** | 土曜日に配置するスタッフ数 |
| **金曜遅番を土曜に優先配置** | ONにすると、金曜日の遅番スタッフを土曜日に優先的に割り当てます |

---

### 3.10 休憩設定

`設定 > 基本設定` の「休憩設定」セクションから、休憩時間の計算ルールを設定できます。

#### 法定休憩（自動適用）

労働基準法第34条に基づき、以下のルールが自動的に適用されます。

| 労働時間 | 休憩時間 |
|----------|----------|
| 8時間超 | 60分 |
| 6時間超〜8時間以下 | 45分 |
| 6時間以下 | なし |

#### 例外休憩ルール

早朝シフトなど、特定の条件で異なる休憩時間を適用できます。

| 項目 | 説明 |
|------|------|
| **有効/無効トグル** | 例外ルールのON/OFF |
| **判定時刻** | この時刻以前に出勤開始するスタッフに適用（デフォルト: 12:00） |
| **休憩時間** | 例外時の休憩時間（0〜120分、デフォルト: 30分） |

> **注意**: 法定休憩の方が長い場合は、法定休憩が優先されます。

#### 表示設定

| 項目 | 説明 |
|------|------|
| **シフトモーダルの時間表示** | 「シフト時間」または「実労働時間（休憩差引き）」で表示を切り替え |
| **Excel出力の実働列** | 「シフト時間」または「実労働時間（休憩差引き）」で出力を切り替え |

---

## 4. トラブルシューティング

**Q. スタッフ用ログインで「認証失敗」と表示される**
> A. 名前の選択ミス、またはアクセスキーの入力ミスが考えられます。管理者に正しいアクセスキーを確認してください。

**Q. 希望休の保存ボタンが押せない**
> A. 変更が検出されていない可能性があります。日付をクリックして希望内容を変更してから再度お試しください。

**Q. 自動生成したシフトに空白（割り当てなし）が多い**
> A. 「必要人数設定」を満たす出勤希望が集まっていないか、対応できるスタッフが不足しています。スタッフの希望休を確認するか、手動でシフトを追加してください。

**Q. 特定の日だけシフトが自動生成されない**
> A. その日がロック済みになっている可能性があります。日ビューでロックアイコンを確認し、解除してから再生成してください。

**Q. スタッフのシフトバーがオレンジ色で表示される**
> A. そのスタッフが希望休を提出しているにもかかわらずシフトに入っている日です。管理者が内容を確認・調整してください。

---

**Q. 管理者が希望休を保存しても「提出済み」にならない**
> A. v2.3.1 より、管理者の保存操作と提出済みの切り替えが分離されました。提出済みにするには「提出済みにする」ボタンをクリックし、確認ダイアログで承認してください。

---

*本マニュアルは Web-Attendance v2.4.0 の仕様に基づいて作成されています。*
